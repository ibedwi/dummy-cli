## Testing the CLI

Install the CLI using global package

```bash
# project root directoy
npm install -g
```

Check if the package is installed

```bash
npm list -g
```

Run the CLI
```bash
dummy-cli
```
